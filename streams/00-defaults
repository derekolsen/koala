# This file describes all required and optional variables and is sourced by
# Koala at runtime. Use it as a template and create a file in this folder
# for each stream. 

# Required
name=Animation
url=https://animejs.com/
destination=127.0.0.1:10023

# Optional (defaults shown here)
width=1920
height=1080
colordepth=24
framerate=30
bitrate=6000
# Note: bitrate is measured in kilobits
